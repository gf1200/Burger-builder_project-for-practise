import React from 'react';

const logo = ({ size }) => (
  <i
    className="material-icons left teal-text text-lighten-5"
    style={{ fontSize: size }}
  >
    playlist_add_check
  </i>
);
export default logo;
